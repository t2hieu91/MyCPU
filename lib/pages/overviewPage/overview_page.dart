import 'package:flutter/material.dart';

class OverviewPage extends StatelessWidget {
  static String routeName = "/overview-page";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Overview Detail'),
      ),
    );
  }
}
